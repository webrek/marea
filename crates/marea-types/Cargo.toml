[package]
name = "marea-types"
description = "Verificador de tipos del lenguaje Marea: resolución, tipos, fronteras de red y uniones."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[lib]
name = "marea_types"
path = "src/lib.rs"

[dependencies]
marea-syntax = { path = "../marea-syntax" }
